export enum Routes {
  Login = '/login',
  Users = '/items',
  Weak = '/items/weak',
  Reused = '/items/reused',
  Root = '/',
}

export enum API {
  Login = 'api/login',
  Logout = 'api/logout',
  Items = 'api/items',
  User = 'api/user',
}
